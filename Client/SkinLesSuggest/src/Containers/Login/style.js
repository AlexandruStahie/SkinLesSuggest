import { StyleSheet } from 'react-native';
import { colors } from '../../utils/consts';

export default StyleSheet.create({
  forgot: {
    color: colors.black,
    fontSize: 11
  },
  simpleText: {
    color: colors.black,
  },
  errorMessage: {
    color: colors.red,
    fontSize: 15,
  }
});
